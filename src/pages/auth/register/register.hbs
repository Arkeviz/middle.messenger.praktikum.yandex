{{#>DsAuthPage formClass="register-form" title="Регистрация"}}
  {{#each fields}}
    {{> DsInput label=label type=type errorMessage=errorMessage }}
  {{/each}}
  {{> DsButton actionType="submit" label="Зарегистрироваться" type="primary" }}
  {{> DsButton actionType="button" label="Войти" type="link" }}
{{/DsAuthPage}}
