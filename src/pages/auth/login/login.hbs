{{#>DsAuthPage formClass="login-form" title="Вход"}}
  {{> DsInput label="Логин" type="text" name="login" errorMessage="Неверный логин" }}
  {{> DsInput label="Пароль" type="password" name="password" }}
  {{> DsButton actionType="submit" label="Авторизоваться" type="primary" }}
  {{> DsButton actionType="button" label="Нет аккаунта?" type="link" }}
{{/DsAuthPage}}
