{{#if (isButtonIcon type)}}
  <button class="ds-button ds-button__icon">
    {{> @partial-block }}
  </button>
{{else}}
  <button class="ds-button ds-button__{{type}}">
    {{label}}
  </button>
{{/if}}
